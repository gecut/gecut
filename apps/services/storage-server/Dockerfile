# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build services--storage-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t services--storage-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system services--storage-server && \
          adduser --system -G services--storage-server services--storage-server

COPY dist/apps/services/storage-server services--storage-server
RUN chown -R services--storage-server:services--storage-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix services--storage-server --omit=dev -f install

CMD [ "node", "services--storage-server" ]
